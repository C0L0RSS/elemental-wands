// Optional real-server regression fixture. All worlds, players and metadata stay under build/.
// ./gradlew -I tools/guardian_church_smoke.init.gradle runServer --args nogui
// ./gradlew -I tools/guardian_church_smoke.init.gradle -PchurchRecovery runServer --args nogui
allprojects {
    plugins.withId('fabric-loom') {
        def smokeRoot = layout.buildDirectory.dir('guardian-combat-smoke').get().asFile
        def runRoot = file('build/guardian-combat-smoke-run')
        def recovery = project.hasProperty('churchRecovery')
        tasks.register('guardianCombatSmokeSetup') {
            doLast {
                runRoot.mkdirs()
                def oldWorld = new File(runRoot, 'combat-test')
                if (!recovery && oldWorld.exists() && !oldWorld.renameTo(new File(runRoot, 'combat-test-before-' + System.currentTimeMillis()))) {
                    throw new GradleException('Could not preserve the previous disposable test world.')
                }
                def acceptedEula = file('run/eula.txt')
                if (!acceptedEula.exists() || !acceptedEula.text.contains('eula=true')) {
                    throw new GradleException('Accept the Minecraft EULA in run/eula.txt before using this test server.')
                }
                new File(runRoot, 'eula.txt').text = acceptedEula.text
                new File(runRoot, 'server.properties').text = '''server-ip=127.0.0.1
server-port=0
online-mode=false
level-type=minecraft:flat
generate-structures=false
level-name=combat-test
generator-settings={"layers":[{"block":"minecraft:bedrock","height":1},{"block":"minecraft:dirt","height":2},{"block":"minecraft:grass_block","height":1}],"biome":"minecraft:plains"}
spawn-protection=0
view-distance=8
simulation-distance=8
difficulty=normal
max-tick-time=-1
'''
                new File(runRoot,'COMBAT_FAILED.txt').delete()
                new File(runRoot,'COMBAT_PASSED.txt').delete()
            }
        }
        tasks.register('guardianCombatSmokeMetadata') {
            doLast {
                def metadata = new File(smokeRoot,'metadata')
                metadata.mkdirs()
                new File(metadata,'fabric.mod.json').text = '''{
  "schemaVersion": 1, "id": "guardian_church_smoke", "version": "1.0.0", "environment": "server",
  "entrypoints": {"main": ["com.anton.elementalwands.entity.GuardianCombatSmokeMod"]},
  "depends": {"elementalwands": "*"}
}'''
            }
        }
        tasks.register('guardianCombatSmokeJar', Jar) {
            dependsOn guardianTestClasses, guardianCombatSmokeMetadata
            archiveFileName = 'guardian-combat-smoke.jar'
            destinationDirectory = smokeRoot
            from sourceSets.guardianTest.output
            from new File(smokeRoot,'metadata')
        }
        loom.runs.server.runDir('build/guardian-combat-smoke-run')
        loom.runs.server.vmArg('-Dfabric.addMods=' + new File(smokeRoot,'guardian-combat-smoke.jar').absolutePath)
        loom.runs.server.vmArg('-Dew.church.recovery=' + recovery)
        tasks.named('runServer') {
            dependsOn guardianCombatSmokeJar, guardianCombatSmokeSetup
            standardInput = System.in
            doLast {
                def marker = new File(runRoot,'COMBAT_PASSED.txt')
                if (!marker.exists() || new File(runRoot,'COMBAT_FAILED.txt').exists()) {
                    throw new GradleException('Church runtime checks did not pass; inspect build/guardian-combat-smoke-run/logs/latest.log')
                }
            }
        }
    }
}
