package com.anton.elementalwands.util;

/** Entity-local transient sprint suppression, mirrored to tracking clients. */
public interface StoneStaggerAccess {
    void elementalwands$staggerUntil(long tick);
}
