# CLAUDE.md

**Read [AGENT.md](AGENT.md) before working in this repository.** It is the single
authoritative guide for this codebase: build, required checks, deploy paths,
package structure, architecture, per-element behavior, manager singletons,
networking, and the workflow for adding or changing abilities.

This file used to carry its own copy of that guidance and drifted out of date —
it described the retired per-element wand classes and a Lunar Client deploy path
that does not exist. Rather than maintain two guides that disagree, keep all
repository guidance in `AGENT.md` and leave this file as the pointer to it.
