package com.anton.elementalwands.data;

public enum WizardAffinity {
    // ICE was retired and re-themed into NATURE (see NatureAbilityHandler). The
    // ICE slot is intentionally left free so a future, differently-abled ice wand
    // can be re-added without colliding with NATURE.
    NONE, FIRE, WIND, STONE, NATURE, SPACE
}
